[project]
name = "online-cinema-fastapi"
version = "0.1.0"
description = "FastAPI-based online cinema project."
authors = [
    {name = "illia",email = "illia.maznitskyi@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi (>=0.115.12,<0.116.0)",
    "black (>=25.1.0,<26.0.0)",
    "flake8 (>=7.2.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
